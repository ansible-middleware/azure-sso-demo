- name: Create Azure VM
  hosts: localhost
  connection: local
  tasks:
    - name: Print a message
      ansible.builtin.debug:
        msg: "this task runs before the example role"

    - name: Create VM's on azure.
      include_role:
        name: 'azure'
      vars:
        ssh_private_key_path: "{{ lookup('file', '/home/hcheruku/.ssh/1643057028_7780352.pub') }}"
